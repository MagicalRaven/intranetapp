export const login = async (email, password) => {
    // Aquí puedes hacer una llamada a la API para la autenticación
    // Por ahora, simularemos un inicio de sesión exitoso
    return { email, role: 'cliente' }; // o 'empleado' dependiendo del usuario
  };
  